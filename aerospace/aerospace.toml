# AeroSpace config — keyboard-driven tiling for macOS.
# https://nikitabobko.github.io/AeroSpace/guide
#
# Modifier is Option (alt). NOTE on the kanata home-row mods in this repo:
# the focus/move keys are hjkl (right hand), and holding right-hand Option
# (home-row `l`) + a right-hand key resolves to a TAP, not Option. So drive
# these bindings with LEFT Option — the physical left Option key, or home-row
# `s` (left ring, opposite hand from hjkl, chords cleanly).

# Start AeroSpace at login.
start-at-login = true

# Normalize the tree so layouts stay predictable.
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# Default layout for new workspaces.
default-root-container-layout = 'tiles'
default-root-container-orientation = 'auto'   # widescreen -> horizontal, tall -> vertical

accordion-padding = 30

# Keep the mouse with the focused window when switching monitors.
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Gaps between windows and screen edges.
[gaps]
inner.horizontal = 8
inner.vertical   = 8
outer.left       = 8
outer.bottom     = 8
outer.top        = 8
outer.right      = 8

# ---------------------------------------------------------------------------
# Main mode
# ---------------------------------------------------------------------------
[mode.main.binding]

# Focus (vim hjkl) — use LEFT Option (see note above)
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# Move the focused window
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# Resize
alt-minus = 'resize smart -50'
alt-equal = 'resize smart +50'
alt-shift-equal = 'balance-sizes'

# Layout toggles
alt-slash = 'layout tiles horizontal vertical'   # switch tiling orientation
alt-comma = 'layout accordion horizontal vertical'
alt-f     = 'fullscreen'
alt-shift-space = 'layout floating tiling'        # float / unfloat the window

# Workspaces (switch)
alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
alt-3 = 'workspace 3'
alt-4 = 'workspace 4'
alt-5 = 'workspace 5'
alt-6 = 'workspace 6'
alt-7 = 'workspace 7'
alt-8 = 'workspace 8'
alt-9 = 'workspace 9'

# Send window to workspace (and follow it)
alt-shift-1 = ['move-node-to-workspace 1', 'workspace 1']
alt-shift-2 = ['move-node-to-workspace 2', 'workspace 2']
alt-shift-3 = ['move-node-to-workspace 3', 'workspace 3']
alt-shift-4 = ['move-node-to-workspace 4', 'workspace 4']
alt-shift-5 = ['move-node-to-workspace 5', 'workspace 5']
alt-shift-6 = ['move-node-to-workspace 6', 'workspace 6']
alt-shift-7 = ['move-node-to-workspace 7', 'workspace 7']
alt-shift-8 = ['move-node-to-workspace 8', 'workspace 8']
alt-shift-9 = ['move-node-to-workspace 9', 'workspace 9']

# Jump back to the previous workspace / bounce a window between monitors
alt-tab = 'workspace-back-and-forth'
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# Reload this file
alt-shift-c = 'reload-config'

# Enter service mode for the less-common stuff
alt-shift-semicolon = 'mode service'

# ---------------------------------------------------------------------------
# Service mode — press alt-shift-; then one key (esc returns to main)
# ---------------------------------------------------------------------------
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main']       # reset window tree
backspace = ['close-all-windows-but-current', 'mode main']

# Join the focused window into a neighbor's container
alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']
